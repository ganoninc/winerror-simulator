define([], function(){ 
    var frozenBackground = (function(){
        var canvas = document.getElementById("frozen-background-simulator");
        
        return {
        };
    }());

    return frozenBackground;
});